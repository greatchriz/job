@aware(['id' => '', 'type' => '', 'placeholder' => '', 'value' => '',])
<input
    type="{{ $type }}"
    id="{{ $id }}"
    name="{{ $name }}"
    placeholder="{{ $placeholder }}"
    value="{{ $value }}"
>
