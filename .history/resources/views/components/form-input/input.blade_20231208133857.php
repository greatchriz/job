@aware(['id' => '', 'type' => '', 'placeholder' => '', 'value' => '', 'name' => ''])
<input
    type="{{ $type }}"
    id="{{ $id }}"
    name="{{ $name }}"
    placeholder="{{ $placeholder }}"
    value="{{ $value }}"
>
